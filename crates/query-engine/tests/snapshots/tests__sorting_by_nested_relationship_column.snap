---
source: crates/query-engine/tests/tests.rs
expression: result
---
SELECT
  row_to_json("universe") AS "universe"
FROM
  (
    SELECT
      *
    FROM
      (
        SELECT
          coalesce(json_agg(row_to_json("rows")), '[]') AS "rows"
        FROM
          (
            SELECT
              "track"."Name" AS "Name"
            FROM
              "public"."Track" AS "track"
              LEFT OUTER JOIN LATERAL (
                SELECT
                  "%ORDER_PATH_PART_1_artist"."Name" AS "name"
                FROM
                  (
                    SELECT
                      "%ORDER_PATH_PART_0_album"."ArtistId" AS "ArtistId"
                    FROM
                      "public"."Album" AS "%ORDER_PATH_PART_0_album"
                    WHERE
                      (
                        true
                        AND (
                          "track"."AlbumId" = "%ORDER_PATH_PART_0_album"."AlbumId"
                        )
                      )
                  ) AS "%ORDER_PATH_PART_0_album"
                  LEFT OUTER JOIN LATERAL (
                    SELECT
                      "%ORDER_PATH_PART_1_artist"."Name" AS "Name"
                    FROM
                      "public"."Artist" AS "%ORDER_PATH_PART_1_artist"
                    WHERE
                      (
                        true
                        AND (
                          "%ORDER_PATH_PART_0_album"."ArtistId" = "%ORDER_PATH_PART_1_artist"."ArtistId"
                        )
                      )
                  ) AS "%ORDER_PATH_PART_1_artist" ON ('true')
              ) AS "%ORDER_0_FOR_track" ON ('true')
            ORDER BY
              "%ORDER_0_FOR_track"."name" ASC
          ) AS "rows"
      ) AS "rows"
  ) AS "universe"

[]
